{
  "name": "infrared-photography-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Infrared Photography Studio (IPS) — unbounded white balance, channel swap, hue shifts for color-IR photos.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
