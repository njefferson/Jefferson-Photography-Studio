# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=89267207db3a7a43
diffhash=9748f9246eeca58a
verdict=IN-SCOPE: the staged diff moves Imported LUTs from Export to Grade exactly as planned, with the breadcrumb, the tab-name fix, and the archived decision record, and nothing else.
# Independent scope judge, reading the plan by path and the staged diff.
# 
# The staged diff (ir.html, NOTES.md, docs/decisions/057-...md) does exactly
# what the plan's five Steps call for and nothing more.
# 
# Step 1/2: the entire Imported LUTs block moved intact from sec-export to the
# end of sec-grade, directly after #mix3Reset, verified word-for-word against
# its pre-image with exactly one added sentence in the new location ("The R⇄B
# swap above runs first, so it composes with whatever you pick below") and one
# new breadcrumb note left in Export. Every lut* id occurs exactly once in the
# file; no id added, removed, renamed or duplicated. One harmless discrepancy:
# an extra <hr class="divider" /> was added in Grade rather than reusing the
# one left behind in Export (hr count 9->10) - functionally a wash, a divider
# still separates content in both locations, not a scope problem.
# 
# Step 4: exactly one word changed in the IR-tab note ("Colour tab" -> "Grade
# tab's channel mixer"), isolated, nothing else in that sentence touched.
# 
# Step 5: docs/decisions/057 carries all seven required sections (six plus
# Outcome, correctly required since this is archived immediately per
# tools/decisions-check.mjs's OPEN_SECTIONS + Outcome rule), decisions-check
# exits 0, its Depends edge (touches 054) is valid and reflected in the graph
# check, and its factual claims about pipeline order match and slightly
# sharpen the plan's own research, verified independently against
# src/pipeline.ts and src/gl.ts rather than invented.
# 
# NOTES.md bullet is under Shipped (roadmap archive), carries the decision
# comment on its first line and a Shown as line that internalWords() from
# tools/reader-words.mjs returns [] against (LUTs plural doesn't match the
# singular lut boundary pattern).
# 
# Nothing else moved: no src/pipeline.ts, src/gl.ts or src/main.ts in the
# diff, matching the plan's explicit "nothing in JS changes." contract-check
# (304/304) and stamp-check both green. No attribution trailer, chat-session
# link or model name; no tables in the new markdown.
