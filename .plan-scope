# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=6cd3cea0d2daea20
diffhash=88cdcaad73556372
verdict=IN-SCOPE: NOTES.md archives 043, 051's Rank is corrected, and its Built already section is the gate's own precondition for the ranking the plan directs; no source files touched
# Judged by a subagent that read the plan BY PATH and the staged diff, three
# times across the changes below.
# 
# NOTES.md moves roadmap item 043 out of the open list to the top of the Shipped
# archive and touches nothing else; 051's record changes inside "## Rank" and
# gains "## Built already". No file under src/ is touched, which is what the plan
# says.
# 
# ONE FINDING WAS ACTED ON. The first pass flagged a clause in the new archive
# entry claiming the walk had been planted with an all-zero bitmap of the right
# length and read zero coverage. That plant WAS run in this session and did read
# 0.00%, but nothing in the repository records it, so a reader of the shipped
# patch note could not find the evidence behind it. The clause was removed rather
# than defended. Recording the plant in tools/keep-walk.mjs's own header is worth
# doing and is a separate change this plan does not authorise.
# 
# THE "## Built already" SECTION IS THE CLOSE CALL, and the watcher said so
# rather than waving it through. The plan's Files list scopes 051's edit to Rank
# only, so on the letter of the plan a stricter reading could refuse it. It was
# judged in scope because the plan's own step 3 directs that 051 leads the queue,
# which makes it the top-ranked item, and decisions-check.mjs refuses any commit
# where the top-ranked record lacks that section (LESSONS 330) and names the
# remedy in its refusal. There was no discretion: the section is a precondition
# of the plan's own directed outcome landing at all, it carries no product
# decision, and it is placed where 043's own record puts the same section.
# 
# ALL SIX CLASSES OF CLAIM IN THAT SECTION WERE CHECKED AGAINST THE FILES and
# every one held: keepstore.ts's five exports at their lines; keepEdit and
# keepCurrentAsFile in main.ts with writeKeepFile in keepfile.ts, and
# keepCurrentAsFile's body read to confirm it joins them into a Blob for the share
# sheet; showLoneWithEdit called from exactly two places, the keep-file path and
# the kept-store path; ir.html's five kept ids and the kept-row class; keepAPhoto
# at a11y-walk.mjs line 181; and kept-walk.mjs reaching the list through #keptOpen
# at exactly two lines.
# 
# ONE FINDING CARRIED, not a scope violation. The archive entry names a THIRD
# defect -- correction strokes lost to JSON.stringify -- where the plan's step 2
# enumerated two. It is accurate and already in 043's Outcome in substance.
# 
# The walks the plan requires ran before this commit, against a served build:
# release-walk all checks passed; keep-walk green with 22.09% painted and 22.09%
# restored; a11y-walk ran to completion over seven pages at two widths in both
# themes, 131 checks ok and exactly two failures, both the recorded instrument gap
# where keepAPhoto cannot arm itself. Nothing else is red.
