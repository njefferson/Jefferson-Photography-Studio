# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=e643191cd7b74f81
diffhash=11e4be98bc8c621d
verdict=IN-SCOPE: the five-file diff implements exactly the four listed JPS items from Close the gates, with no drift and no pronoun or attribution violations
# Unit 4, the JPS items: decisions-check refuses chat provenance and credit to a reader or a role in
# every record (each pattern planted and refused; "the reader wants" left legal for readers in general);
# notes-check counts what notes.html renders through the build's own Shown-as filter and refuses zero
# (planted and refused; 5 rendered, matching the built page); the hub pin and .doctrine-sync moved
# together to 5c57d1f; CLAUDE.md's staging check is by content; notes-check's header names the shape it
# cannot see. Judged by an independent subagent that read the plan by path.
