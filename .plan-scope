# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=3bccd3e5ce5e4dc3
diffhash=a8b1017061b3dcae
verdict=IN-SCOPE: the plan's step 3 exactly — the 045 bullet moved and ticked with a three-line shipped marker, the record's Outcome appended, nothing else touched
# Staged: NOTES.md and the 045 record only. Nothing else in `git status -uall`.
# 
# The 045 bullet appears 0 times in the roadmap section and once in the archive,
# ticked. Roadmap holds 0 ticked boxes; the archive holds 0 unticked. Open
# roadmap 23, archive 117 -- both as the plan states. Ranks by decision key:
# 12 -> 015, 13 -> 046, 14 -> 002, 15 -> 003.
# 
# The body is a PURE MOVE, proved rather than asserted. `git diff --cached -U0`
# gives exactly two hunks, @@ -849,25 +848,0 @@ and @@ -3422,0 +3398,28 @@; 25 out,
# 28 in. The difference is the bullet line (`- [ ]` -> `- [x]`, verified by string
# substitution) plus three marker lines. The remaining 24 body lines are
# byte-identical and in the same order.
# 
# The record's diff is a pure append. Headings: Context, Looked up, Weighed
# against, Depends, Options, Rejected, Rank, Outcome -- all non-empty.
# 
# ON THE PLAN'S OWN PREDICTION, which was wrong: the plan said the Outcome would
# record that the head order came out `039, 038, 024, 040, 016` rather than the
# `038, 039, 040` the record's reasoning predicted. The record predicts no such
# order -- it mentions 039 and 038 only at lines 5, 23 and 136, all about the
# defect demonstrating itself while those two were being archived. The Outcome as
# written claims nothing of the kind; it says the head came out
# `045, 039, 038, 024, 040, 016`, which matches the measured keys, and that this
# is the kind of inversion the freshness gate was built to tolerate. The plan's
# error was not copied into the repository.
# 
# The marker names the commit by SUBJECT because the rebase merge mints the SHA,
# and says so in the file rather than guessing one. VERSION reads 2.58, unchanged.
