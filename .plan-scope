# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=000842213c6d61a4
diffhash=f1b1c0387e351341
verdict=IN-SCOPE: This is exactly the "Now" section's step 1 / item 1d "Fixed:" commit — `addMask` in src/main.ts now leaves `neutralMask`'s values as-is (removing the old brightness/saturation defaults), and gl.ts's Renderer uploads the mask group the coverage tint is showing even when it changes nothing, preview-only via `u_maskViz`/`maskGroupsForRender`, leaving compileEdit/export untouched — precisely as item 1d and "Now" step 1 specify. The walk changes match too: mask-slots-walk gains section C (fresh radial/gradient/Sky mask leave the photo unchanged, plus the new-radial tint-while-placed check) and its closing line drops the atlas-blaming language; aim-walk and agreement-walk get their stale "arrives at 1.3" comments corrected while keeping their explicit neutralizing calls; look-roundtrip-walk's "with a Sky mask" arm now sets saturation 1.3 explicitly so it still tests a rendering mask. The docs/ARCHITECTURE.md diff is the regenerated line counts. No PREVIEW_PIPELINE bump is needed: the preview-version gate's hashed sources do not include gl.ts or addMask.
# IN-SCOPE: This is exactly the "Now" section's step 1 / item 1d "Fixed:" commit — `addMask` in src/main.ts now leaves `neutralMask`'s values as-is (removing the old brightness/saturation defaults), and gl.ts's Renderer uploads the mask group the coverage tint is showing even when it changes nothing, preview-only via `u_maskViz`/`maskGroupsForRender`, leaving compileEdit/export untouched — precisely as item 1d and "Now" step 1 specify. The walk changes match too: mask-slots-walk gains section C (fresh radial/gradient/Sky mask leave the photo unchanged, plus the new-radial tint-while-placed check) and its closing line drops the atlas-blaming language; aim-walk and agreement-walk get their stale "arrives at 1.3" comments corrected while keeping their explicit neutralizing calls; look-roundtrip-walk's "with a Sky mask" arm now sets saturation 1.3 explicitly so it still tests a rendering mask. The docs/ARCHITECTURE.md diff is the regenerated line counts. No PREVIEW_PIPELINE bump is needed: the preview-version gate's hashed sources do not include gl.ts or addMask.
