# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=0ca28ae701655148
diffhash=66caa798ebac0a40
verdict=IN-SCOPE: the ceiling probe plus a regenerated module-map line count and the gate's own ledger, with none of the plan's later export-path work present.
# Independent scope judge, reading the plan by path and the staged diff.
# 
# Three files: src/debug.ts, docs/ARCHITECTURE.md and .plan-scope. No product
# code beyond the probe. No src/export.ts, no src/exportparallel.ts, no
# src/main.ts, no decision record.
# 
# The module map's change is one line — src/debug.ts's generated line count moving
# 1288 to 1441 — and the arithmetic was checked rather than taken on trust: the
# two hunks add 152 and 1 lines, and 1288 plus 153 is 1441. The description text
# is unchanged. .plan-scope is the gate's own ledger, rewritten wholesale on every
# recorded verdict, as its header declares.
# 
# src/debug.ts has two hunks: the new probe function, placed between its two
# siblings, and one call in the existing run sequence beside them. No other
# function, control or call site is touched.
# 
# None of the plan's later items are present — the Promise fix, the loud fallback,
# the TIFF copy, the export path's own drawing-surface defects, the spots
# exclusion and the ratchet are all absent. The probe measures the two caps the
# plan says those lines violate and modifies none of them.
# 
# Read-only, self-cleaning and bounded, checked in the new code rather than
# assumed: no storage or app-state write anywhere, a release after every draw in
# part one, a finally covering the early break in part two, six fixed sizes and a
# hard stop at seven frame-sized surfaces.
