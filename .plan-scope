# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=0ca28ae701655148
diffhash=a9fd1c25c763b146
verdict=IN-SCOPE: items A and B of the approved increment release, with C, D, the spots exclusion and the ratchet all confirmed absent, plus a regression walk for the defect being fixed.
# Independent scope judge, reading the plan by path and the staged diff.
# 
# Implements A and B and nothing beyond them. A is forTheWire, a named-field
# plain object replacing the live decoded image at both post sites, with the sky
# selection and its promise correctly left out. B is the recorded reason beside
# the live thread count, its accessor, its field on the profile, and the two
# reader-facing lines that carry it.
# 
# The derived ProfileMs type is part of B rather than drift: adding a string field
# to the profile stopped the timing marks compiling, and deriving the markable
# keys from the interface is the narrow fix.
# 
# C is confirmed absent: the TIFF branch still sets into a pre-allocated buffer.
# D is confirmed absent: nothing near the drawing-surface block is touched. No
# change to the parallel-export module, the spots exclusion or any ratchet, by
# grep over the diff.
# 
# The module map carries only two regenerated line counts, no prose and no new
# entries.
# 
# The new walk is judged in scope rather than creep: it asserts the exact property
# A establishes, on the batch path the research showed was the broken one, in the
# repo's own walk shape, and this repo requires a new test to fail once before it
# is trusted — which it did, three times over, against the planted defect.
