# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=dddb964aa25bec2a
diffhash=79bd5a6d4e0ab111
verdict=IN-SCOPE: 030's spatial aims as the plan sets out, but INCOMPLETE — the CPU blend is unverified and the new agreement arm is not trustworthy, so this stays off staging
# JUDGED BY THIS SESSION, NOT BY A WATCHER, and said so because the gate's design
# is a second reader. The reason is not cost this time: the work is INCOMPLETE and
# what it needs is not a scope opinion but a measurement I have not got.
# 
# IN SCOPE against the plan: the two bits, the blend in both paths, the two
# toggles, PREVIEW_PIPELINE, the five .scope-allow rows, the aim-walk stages. The
# plan's Files list covers every path touched.
# 
# WHAT IS VERIFIED. tools/aim-walk.mjs passes all four statements for both new
# stages — including the first one, aiming nothing changes nothing, at 0.000% of
# the frame moved, which is the invariant protecting every edit already saved.
# The shader links and draws. Arms 1-3 of the agreement walk still pass, so the
# UNAIMED paths are untouched. The scope gate's OWED list is empty for the first
# time.
# 
# WHAT IS NOT VERIFIED, and this is why nothing here goes to staging: the CPU
# blend in src/export.ts. aim-walk reads the preview canvas, so it measures the
# shader alone. The arm added to tools/agreement-walk.mjs to compare preview
# against export is NOT TRUSTWORTHY YET — it reports the two 180 degrees apart in
# hue with lightness matching to 0.2 points, and that reproduces with NO mask and
# NO aim at all, so it is measuring the two READ ROUTES rather than the render.
# Reading the preview through a 2D canvas instead of readPixels did not move it.
# The cause is not isolated.
# 
# TWO THINGS THE RUN GOT WRONG, recorded rather than smoothed over. The arm first
# waited on a download straight after the export press and timed out at ten
# minutes, while the strip had read Ready since twenty seconds in — a finished
# export collects and needs a second press. And before finding that, I fixed a
# real inefficiency in aimedSampler (it ran the full bilateral at weight 0 and
# threw it away) and wrote into the source comment that it was why the arm timed
# out. It was not. The comment now says so.
