# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=4c24ae676c8c146e
diffhash=1291ee6b4caa251e
verdict=IN-SCOPE: every hunk implements record 064's chosen option (None first, one undo step back to the photograph's own balance with no creative grade, session look cleared, state in words, LUT, Reset and Default look untouched) and adds nothing else; no owner quotation.
# Commit: the plan's third 062-section bullet, "064's None button for looks follows as its own commit, per 064's rank, once 062 is in" (062 is in as fd2382d on the held candidate branch). Scope is record 064's chosen option.
# - ir.html: #lookNone first in .looks-grid, with a comment on why; one sentence added to the looks note saying what None does and that an imported LUT stays.
# - src/main.ts: lookNoneBtn; updateLookUI marks None pressed (class active, the same as the other look buttons) with its state in words ("no look on" / "take it off"); takeLookOff: one undo step (flush before and after); divides the look's white-balance bias out and hands a camera file's look-applied balance back, using applyLook's own rules; every creative field applyLook assigns goes to the value applyLook uses when a look carries none (which is also the editor's initial value); denoise floor and texture handed back only if unmoved; swap to freshBaseline's; Restore depth re-solved as a look-less open does; activeLook and sessionLook to null; the Finish panel closed; and the syncToUI/syncFromUI round trip the open makes, so the frame lands exactly on the open's. The LUT is not touched; Reset and Settings' Default look are not touched. With no look on, None only clears the session look.
# - docs/ARCHITECTURE.md: generated line count.
# Evidence: a scratch walk through the real build: a raw opened with no look shows None pressed in words; Aerochrome changes the frame; None returns it to the byte-identical frame the photograph opened with (this failed until the round trip was added: the tone points differed in the third decimal, found by comparing the editor's full snapshot in a scratch build that was then reverted); one Undo puts the look back and Redo takes it off; a LUT stays; the next photograph in a set opens without a look; a camera JPEG returns to its byte-identical open frame. The looks grid was opened in screenshots in both states. Control, look round-trip, one-band, tile-truth and a11y walks run on this build.
# Not in this commit: anything of 060, the start card, Reset's meaning, or the Default look setting.
