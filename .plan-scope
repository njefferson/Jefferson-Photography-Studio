# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=6cd111fa1a897dbe
diffhash=8403f709173705af
verdict=IN-SCOPE: step 1 of the promotion plan, the one-line VERSION bump to 2.61 as the release's final commit, with nothing else needing to move alongside it.
# Independent scope judge, reading the plan by path and the staged diff.
# 
# One file, one line: VERSION 2.60 to 2.61. Nothing else staged or modified.
# 
# The value checks out against the repo's own rule rather than against a guess:
# the taxonomy is identity, capability, increment, the middle number bumps on
# every capability release, and the edit rides the release's final commit so that
# commit displays as exactly the new base. The ten commits above main are
# capability work.
# 
# VERSION is matched by INTERNAL_PATH in vite.config.ts, the same pattern the
# patch-note gate reads, so an Internal-prefixed subject keeps it out of the in-app
# notes.
# 
# Nothing else moves with it, checked rather than assumed: the service worker's
# cache name is a build-time placeholder stamped from this file, package.json's
# version is unrelated npm metadata, every occurrence of the old number across the
# source, tools, records and notes is narrative history that the versioning rule
# says is not renumbered, and no gate or fixture hardcodes a version string.
