# Written by plan-scope-check.mjs --record. Not hand-edited.
plan=/root/.claude/plans/linear-baking-island.md
planhash=3bccd3e5ce5e4dc3
diffhash=a3415ad6fb495371
verdict=IN-SCOPE: the plan's step 6 exactly — a byte-identical replay of edf92c2, the two docs files only, verified to reach neither the build nor the deployed site
# Byte-identical replay of edf92c2. `git diff edf92c2 --cached --` over the two
# files is 0 bytes, exit 0; blob hashes match on both (IR-SCIENCE.md c7926a9,
# 034 d793c04).
# 
# Disjoint from the first two commits of this branch. b9e87f6 touches
# .plan-scope, NOTES.md, the 045 record, tools/notes-check.mjs and vite.config.ts;
# 1cf26e0 touches .plan-scope, NOTES.md and the 045 record. Neither names
# IR-SCIENCE.md or the 034 record.
# 
# decisions-check exits 0. 034 is not in .looked-allow (the backlog is 001, 017,
# 021), so it was checked in full: 13 NIR_ frames named anywhere in the record,
# the same 13 inside "## Looked at", set difference empty in both directions. The
# `touches 019` line covers the new prose citing 019, so the undeclared-pairs
# count stays at its allowed 32.
# 
# DOCS-ONLY VERIFIED rather than assumed, so no device pass is owed.
# vite.config.ts reads exactly two files at build time, ./VERSION and ./NOTES.md;
# it never reads IR-SCIENCE.md or anything under docs/decisions. Its own
# INTERNAL_PATH deny-list matches both staged paths twice over (the docs/ root
# and the .md extension), so neither can reach the ⓘ patch notes. Every hit for
# IR-SCIENCE under src/ is a prose citation inside a comment -- no import, no
# fetch, no ?raw, no import.meta.glob -- and there are zero references to
# docs/decisions in src/ or public/. No .md file exists under public/ and none
# appears in dist, so nothing here is precached or served.
# 
# Nothing else staged, nothing unstaged, no untracked files.
